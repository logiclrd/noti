package root

var helpText = `noti [options] [notification [options]] [command]

OPTIONS
    -verbose
        Enable verbose mode.
    -h, -help
        Show help.

NOTIFICATIONS
    banner
    slack
    speech
`
